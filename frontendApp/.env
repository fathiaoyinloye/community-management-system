# Set to false to connect to the actual backend API rather than running in-memory mock mode
VITE_USE_MOCK=false

# If your backend runs locally (e.g. Spring Boot on port 8080), uncomment the line below:
# VITE_API_BASE_URL=http://localhost:8080
